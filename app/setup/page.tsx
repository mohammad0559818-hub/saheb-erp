import { SetupWizard } from "@/components/setup/setup-wizard";

export default function SetupPage() {
  return (
    <main className="min-h-screen bg-slate-100 px-6 py-12" dir="rtl">
      <SetupWizard />
    </main>
  );
}
